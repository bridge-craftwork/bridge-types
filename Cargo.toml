[package]
name = "bridge-types"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "Core bridge types: Card, Hand, Deal, Board, Contract, and evaluation methods"
repository = "https://github.com/Rick-Wilson/bridge-types"

[dependencies]
# No external dependencies - pure Rust types
